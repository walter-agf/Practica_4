"""
x = (float(input("Tratar nombrar = ")))
y = int(x)
z = float(y)
print(x)
print(y)
print(z)
a = 1
b = 2
print(a+b)
"""
"""
b = len (a) - 1
            while lis(num_two) > a (central) and t = False:
                if lis(num_two) > a (b):
                    t = True
                    a (b).append lis(num_two)
                else :
                    b -= 1
"""
a = []
        a.append (lis (num_two))
        for d in range (num_two):
            t = False
            num_two -= 1
            central = a // 2
            if lis(num_two) >= a(central) and lis(num_two) <= a (central + 2) or lis(num_two) <= a(central) and lis(num_two) >= a (central - 1):

            while lis(num_two) > a (central) and t = False:
                a = 
            while lis(num_two) < a (central) and t = False:
